{
  "name": "browser",
  "version": "1.0.0",
  "description": "",
  "main": "test.js",
  "scripts": {
    "test": "npm-run-all -p test:build test:run",
    "test:build": "esbuild index.ts --bundle --outfile=test.js --watch --format=esm",
    "test:run": "npx live-server --port=8081"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.1"
  }
}
